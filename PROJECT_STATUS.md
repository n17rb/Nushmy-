# حالة المشروع — نشمي

آخر تحديث: 2026-09-20 · النسخة 0.6.0 · المراحل المنجزة: **الزبون + الكابتن + المحفظة + الإدارة + المحادثات + الإشعارات + كتالوج السيارات**

---

## ✅ منجز ومُختبَر

### الأساس
- خادم Node.js بدون أي مكتبة خارجية (`node:http`) — يقلل مخاطر التحديثات ويسرّع النشر
- محوّل قاعدة بيانات يعمل بنفس الكود على **PostgreSQL** (Neon/الإنتاج) و**SQLite** (التجربة المحلية)
- مخطط قاعدة بيانات كامل مع مفاتيح أجنبية وفهارس وقيود
- كل المبالغ أعداد صحيحة بوحدة **الفلس** — لا أرقام عشرية في أي عملية مالية
- الصور والوثائق وإشعارات الشحن محفوظة **داخل قاعدة البيانات** — ما بتنمسح مع إعادة تشغيل Render
- سجلات منظّمة (Structured logs) ولا تُسجَّل رموز التحقق في الإنتاج

### الدخول والحساب
- دخول برقم الهاتف + رمز تحقق (4 أرقام، صلاحية دقيقتان)
- تطبيع أرقام الأردن: `07…` و`+962…` و`00962…` و`7…` كلها تُقبل
- الرمز يُخزَّن مجزّأً بـ scrypt، وله حد محاولات وحد إرسال
- جلسات بـ JWT + Refresh Token في كوكي HttpOnly، مع تدوير الجلسة عند التجديد
- الملف الشخصي: الاسم، البريد، **صورة الحساب**
- المظهر: فاتح / داكن / حسب النظام

### الرحلة
- **خرائط بستايل أوبر** مع مسار مرسوم، سيارات تتحرك بنعومة، ودبوس تحديد
- السيارات القريبة على الرئيسية تأتي من كباتن حقيقيين متصلين فقط
- الأماكن المحفوظة والوجهات الأخيرة
- **محرك التسعير** من قاعدة البيانات، وعرض السعر قبل التأكيد لثلاثة أنواع مركبات
- **آلة حالات الرحلة** بـ 12 حالة، ترفض أي انتقال غير منطقي على الخادم
- **محرك التوزيع الذكي**: Score مركّب + دفعات + توسيع تدريجي للنطاق
- الإلغاء مع احتساب الرسوم على الخادم، التقييم بالنجوم والوسوم
- منع رحلتين نشطتين لنفس العميل (معاملات لمنع التزاحم)

### تطبيق الكابتن (`/captain`)
- تسجيل الكابتن والسيارة، ورفع الوثائق (خاصة، لا تُعرض إلا للإدارة)
- اتصال/فصل، إرسال الموقع المباشر، العرض بصوت واهتزاز ويعرض **صافي الربح والوجهة قبل القبول**
- قبول آمن: إذا ضغط كابتنان بنفس اللحظة، واحد فقط ينجح (مُختبَر)
- ملاحة Google Maps / Waze، «وصلت» مرفوضة إذا كان بعيداً أكثر من 400 م
- عدّاد الانتظار، عدم حضور الزبون، «اسحب للتأكيد» للبدء والإنهاء
- العمولة تُخصم من المحفظة مرة واحدة فقط، واعتذار الكابتن يرجّع الطلب للبحث تلقائياً
- الأرباح، الهدف اليومي، المحفظة، طلب شحن عبر CliQ مع صورة الإشعار

### لوحة الإدارة (`/admin`)
- الدخول برقم المالك من `ADMIN_PHONES` (يصير «مدير عام» تلقائياً) أو أي مشرف يضيفه المالك
- **5 أدوار بصلاحيات**: مدير عام، مسؤول مالي، مسؤول تشغيل، دعم فني، مراقب — مفروضة على الخادم
- لوحة القيادة: أرقام اليوم، رسم آخر 7 أيام، تنبيهات المعلّق، آخر الطلبات، خريطة مصغّرة
- الخريطة الحية: الكباتن المتصلين (متاح/برحلة) والطلبات الجارية، كل 5 ثوانٍ
- الرحلات: بحث، فلترة، التفاصيل كاملة (المراحل، العروض المرسلة للكباتن، المبالغ)، إلغاء إداري
- الكباتن: موافقة/رفض/إيقاف/حظر مع سبب، فصل فوري، مراجعة الوثائق بالصور، تعديل الرصيد
- شحن المحافظ: صورة الإشعار + موافقة **ما بتنحسب مرتين** حتى لو انضغطت مرتين (مُختبَر) + رفض بسبب
- العملاء: بحث، تفاصيل، إيقاف/حظر مع طرد فوري من الجلسات
- الأسعار: تعديل كل البنود لكل نوع مركبة بكل منطقة، مع مثال مباشر، وحدود أمان على الخادم
- الإعدادات: العمولة، أدنى رصيد، اسم CliQ، إعدادات التوزيع والإلغاء، تفعيل المناطق
- **سجل العمليات**: كل إجراء حساس (مين، إمتى، قبل/بعد، السبب) — لا يُحذف
- **واتساب**: فحص إعداد Meta خطوة بخطوة مع السبب الحقيقي لأي رفض، ربط استقبال الرسائل، إنشاء القالب، ورمز تجربة

### الدخول بواتساب (`SMS_PROVIDER=whatsapp_link`)
- **الإعداد والتشغيل من لوحة الإدارة** («التحقق من الرقم»): 4 خطوات، App Secret بينحفظ من اللوحة، رمز ربط الـ Webhook بيتولّد لحاله، وزر تشغيل ما بيشتغل إلا إذا كل إشي ✓
- الزبون بيكبس زر بيفتح واتساب برسالة جاهزة فيها رمز من 6 أرقام لرقم نشمي، وأول ما توصل منفتحله الجلسة لحالها
- الرسائل الواردة موقّعة ومتحقق منها (App Secret)، والرقم لازم يطابق الرقم الي انكتب، والطلب بيفتح جلسة مرة وحدة بس
- ردود تلقائية بالعربي (تم التأكيد، رقم غلط، رمز منتهي، شرح)، وطلب «حذف حسابي» بينسجل بسجل العمليات
- صفحة سياسة الخصوصية على `/privacy`
- احتياط SMS للي ما عنده واتساب: رسائل من تلفون أندرويد برقمك (تطبيق SMS Gateway for Android المجاني)، مع زر تجربة بلوحة الإدارة
- حماية: ما بينفع إيقاف حساب المالك أو إزالة صلاحيته، ولا إيقاف الكابتن وهو برحلة جارية

### الجديد بالنسخة 0.6
- **المحادثات**: الزبون ↔ الكابتن خلال الرحلة (بدون أرقام، مع ردود جاهزة)، الزبون/الكابتن ↔ الإدارة (الدعم الفني)، ورد تلقائي
- **إبلاغ عن كابتن** (أسباب جاهزة، سرّي) و**غرض منسي** (من «رحلاتي» ← الرحلة) — الإدارة بتراسل الكابتن بكبسة
- **الإشعارات**: صندوق دائم بالتطبيقين + إشعار فوري على التلفون (Web Push مبني بدون مكتبات، وFirebase لتطبيقات المتجر) + رسائل جماعية بكود خصم
- **كتالوج السيارات**: 34 شركة و270+ فئة («الفئة» بدل «الموديل») + اللون والسنة وناقل الحركة
- **بطاقة السيارة للزبون**: صورة الفئة الي بترفعها الإدارة + «2020 | أبيض» مع دائرة بلون السيارة + المقاعد والناقل والوقود + اللوحة. ما في صورة ← بدون صورة + **تنبيه للإدارة**
- **المكالمة داخل التطبيق** بين الزبون والكابتن: صوت عبر الإنترنت بدون رصيد وبدون ما يبين رقم حدا، مع رنّة وإشعار، ورد/رفض، وكتم، وعدّاد، ومكالمة فايتة بتنكتب بالمحادثة
- **تنبيهات الإدارة**: صفحة «التنبيهات» بجرس وعدّاد، وإشعار على تلفونك
- **الخريطة**: إصلاح «API KEY REQUIRED» (CARTO صارت تطلب مفتاح) ← OpenStreetMap بأسماء عربية، أو CARTO بمفتاح من الإعدادات
- **البحث المحسّن**: محلات ومطاعم وشركات (Photon + Nominatim + أماكن الإدارة + الوجهات السابقة)، «عبدلي» = «العبدلي»، الأقرب أول
- **الدقة والتتبع**: GPS بيتحسّن لحد ~15 م مع دائرة الدقة، دبوس الانطلاق بزوم 18.5، ملاحظة للكابتن، الوقت المتوقع والمسافة بالمتر، موقع الزبون المباشر بيوصل للكابتن وهو جاي
- **إصلاحات**: أيقونات الزبون الي فوق ما بتختفي بعد تحميل الخريطة، والتطبيقات بتفتح فاتح (أبيض) دايماً

### الجودة
- **اختبارات وحدات وتكامل** تغطي الزبون والكابتن والإدارة (`npm test`)
- كل خطأ له كود واضح ورسالة عربية مفهومة
- PWA مع عامل خدمة يخزّن الهيكل فقط

---

## ⏳ غير منجز بعد (المراحل القادمة)

| المرحلة | المحتوى |
|---|---|
| 10 | تغليف التطبيقات للمتاجر (Capacitor) وربط Firebase |
| 11 | تقارير مالية قابلة للتصدير (Excel) |
| — | تطبيق الكوبونات على الأجرة تلقائياً، الحجز المسبق، المحطات المتعددة، المناطق (Zones) |

---

## ⚠️ حدود معروفة — بصراحة

1. **تطبيق الكابتن موقع ويب:** لازم يضل مفتوح والشاشة شغالة حتى يوصل الموقع والطلبات.
   هذا بينحل لما نحوّله لتطبيق حقيقي على App Store.
2. **الدخول بواتساب:** Meta ما بتسمح بقالب «رمز دخول» قبل توثيق النشاط التجاري (سجل تجاري)، فالطريقة المعتمدة
   إن الزبون بيبعت الرمز لرقم نشمي (مجانية ومُختبرة). بتحتاج Webhook شغال والتطبيق منشور، وعلى خطة Render المجانية
   أول دخول بعد فترة سكون ممكن يتأخر لحد ما الخادم يصحى.
3. **المسافة المستخدمة في السعر تقديرية** (هوائية × 1.35). خط المسار من خادم OSRM التجريبي.
4. **ترخيص الخرائط.** بدون مفتاح منستخدم OpenStreetMap (سياسة الاستخدام بتسمح بالاستخدام الخفيف). مع زيادة الزباين حط مفتاح CARTO المجاني (5 مليون بلاطة/شهر) من الإعدادات.
8. **كود الخصم بالعروض** بينبعت للزباين كنص — لسا ما بينخصم تلقائياً من الأجرة.
9. **المكالمة داخل التطبيق** بتشتغل بين تلفونين على الإنترنت مباشرة. بشبكات الموبايل المقفلة بتحتاج خادم TURN — حط بياناته من الإعدادات (بدونه ممكن بعض المكالمات ما تربط، وبيطلع «تعذّر الاتصال — جرّب اتصال عادي»). على الآيفون لازم التطبيق يكون مضاف للشاشة الرئيسية حتى تشتغل الرنّة والمايك.
10. **دقة 10 م** بتعتمد على GPS التلفون (بالعادة 5–15 م بالمكان المفتوح). التطبيق بيطلب أعلى دقة، وبيطلب من الزبون يثبّت الدبوس إذا الدقة ضعيفة.
5. **التحديث بالاستعلام الدوري** وليس WebSocket. كافٍ الآن.
6. **حد الطلبات في الذاكرة** — يعمل على خادم واحد فقط.
7. **الدفع نقداً فقط.**

---

## 🔑 المتغيرات المطلوبة للإنتاج

| المتغير | مطلوب؟ | الوصف |
|---|---|---|
| `NODE_ENV` | نعم | `production` |
| `JWT_SECRET` | نعم | قيمة عشوائية ≥ 24 حرفاً |
| `DATABASE_URL` | نعم | رابط PostgreSQL من Neon |
| `ADMIN_PHONES` | نعم | رقم/أرقام المالك — مدير عام دائم للوحة الإدارة |
| `SMS_PROVIDER` | نعم | `whatsapp_link` (الزبون بيبعت الرمز — بدون قالب) أو `whatsapp` (قالب، بده توثيق) |
| `WHATSAPP_VERIFY_TOKEN` | مع whatsapp_link | كلمة سر ربط الـ Webhook |
| `WHATSAPP_APP_SECRET` | مع whatsapp_link | للتحقق من توقيع رسائل Meta |
| `SMS_FALLBACK` | اختياري | `android` = زر «ما عندي واتساب» بيبعت SMS من تلفون أندرويد برقمك |
| `SMSGATE_USERNAME` / `SMSGATE_PASSWORD` | مع android | من تطبيق SMS Gateway for Android |
| `WHATSAPP_TOKEN` | مع واتساب | رمز الوصول الدائم (System User) — سرّي |
| `WHATSAPP_PHONE_NUMBER_ID` | مع واتساب | من صفحة API Setup |
| `WHATSAPP_TEMPLATE` | مع واتساب | اسم قالب المصادقة (افتراضي `nashmi_otp`) |
| `WHATSAPP_WABA_ID` | مع واتساب | معرّف حساب واتساب — لصفحة «واتساب» بلوحة الإدارة |
| `OTP_DEV_SHOW` | للمعاينة فقط | `1` = الرمز يظهر على الشاشة. **احذفه قبل الإطلاق** |
| `CAPTAIN_AUTO_APPROVE` | للمعاينة فقط | `1` = الكابتن يتفعّل بدون موافقتك. احذفه لتوافق من اللوحة |
| `SERVICE_ALL_JORDAN` | مؤقت | `1` = الخدمة بكل الأردن، بدونه الكرك فقط |
| `TWILIO_*` | عند استخدام Twilio | بيانات الحساب |
| `MAP_TILES_URL` / `MAP_TILES_URL_DARK` | لا | لتبديل مزوّد الخرائط |
| `CARTO_API_KEY` | لا | ستايل خرائط رمادي متل أوبر (أو من الإعدادات بلوحة الإدارة) |
| `FCM_SERVICE_ACCOUNT` | لتطبيقات المتجر | ملف Service Account من Firebase |
| `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` | لا | بتتولّد لحالها — اختياري تثبيتها |
| `ROUTING_URL` | للإنتاج | خادم مسارات OSRM خاص |

---

التفاصيل الكاملة في [`README.md`](./README.md).
