# حالة المشروع — نشمي

آخر تحديث: 2026-09-19 · المراحل المنجزة: **تطبيق الزبون + تطبيق الكابتن + المحفظة (سجل الحركات)**

---

## ✅ منجز ومُختبَر

### الأساس
- خادم Node.js بدون أي مكتبة خارجية (`node:http`) — يقلل مخاطر التحديثات ويسرّع النشر
- محوّل قاعدة بيانات يعمل بنفس الكود على **PostgreSQL** (Neon/الإنتاج) و**SQLite** (التجربة المحلية)
- مخطط قاعدة بيانات كامل: 26 جدولاً مع مفاتيح أجنبية وفهارس وقيود
- كل المبالغ أعداد صحيحة بوحدة **الفلس** — لا أرقام عشرية في أي عملية مالية
- سجلات منظّمة (Structured logs) ولا تُسجَّل رموز التحقق في الإنتاج

### الدخول والحساب
- دخول برقم الهاتف + رمز تحقق (4 أرقام، صلاحية دقيقتان)
- تطبيع أرقام الأردن: `07…` و`+962…` و`00962…` و`7…` كلها تُقبل
- الرمز يُخزَّن مجزّأً بـ scrypt، وله حد محاولات وحد إرسال (45 ثانية بين طلب وآخر، 6 في الساعة)
- جلسات بـ JWT + Refresh Token في كوكي HttpOnly، مع تدوير الجلسة عند التجديد
- الملف الشخصي: الاسم، البريد، **صورة الحساب** (تُصغَّر في المتصفح قبل الرفع)
- المظهر: فاتح / داكن / حسب النظام — يُحفظ في الحساب وعلى الجهاز

### الرحلة
- **خرائط بستايل أوبر**: خريطة أساس هادئة (رمادي فاتح نهاراً، فحمي ليلاً)، خط مسار أسود
  سميك يُرسم بحركة، انطلاق دائري ووجهة مربّعة مع بطاقات صغيرة، دبوس تحديد يرتفع أثناء السحب،
  سيارات من الأعلى تدور حسب الاتجاه وتتحرك بنعومة، وحلقات نابضة أثناء البحث عن كابتن
- السيارات القريبة على الرئيسية تأتي من كباتن حقيقيين متصلين فقط (بدون اسم أو رقم، والموقع مقرّب للخصوصية)
- تحديد الموقع الحالي، البحث بالاسم، والتحديد بالدبوس على الخريطة
- الأماكن المحفوظة (البيت / الشغل / مخصص) والوجهات الأخيرة
- **محرك التسعير**: أساس + لكل كم + لكل دقيقة + حد أدنى + مضاعف ذروة، كله من قاعدة البيانات
- عرض السعر والمسافة والزمن **قبل** تأكيد الطلب، لثلاثة أنواع مركبات
- **آلة حالات الرحلة** بـ 12 حالة، ترفض أي انتقال غير منطقي على الخادم
- **محرك التوزيع الذكي**: مرشحون حقيقيون + Score مركّب (ETA، الموثوقية، مدة الانتظار، الاتجاه، الحِمل) + دفعات + توسيع تدريجي للنطاق
- الإلغاء مع احتساب الرسوم على الخادم، وعرض الرسوم للعميل قبل التأكيد
- سجل الرحلات والتقييم بالنجوم والوسوم
- منع رحلتين نشطتين لنفس العميل (تحقق على الخادم، ومعاملات لمنع التزاحم)

### الجودة
- **37 اختباراً** تمر كلها: وحدات + تكامل عبر الـ API
- كل خطأ له كود واضح ورسالة عربية مفهومة (لا "حدث خطأ ما")
- التطبيق يعمل كـ PWA مع عامل خدمة يخزّن الهيكل فقط (لا يخزّن بيانات حية)
- شريط تنبيه عند انقطاع الإنترنت، وإعادة المحاولة تلقائياً

---

### تطبيق الكابتن (`/captain`)
- تسجيل الكابتن والسيارة، ورفع الوثائق (رخصة، هوية، رخصة سيارة) — الوثائق لا تُعرض للعامة
- زر «ابدأ» للاتصال، إرسال الموقع المباشر كل 4 ثوانٍ ونبضة كل 15 ثانية، وإبقاء الشاشة مضاءة
- العرض يوصل مع صوت واهتزاز وعدّاد تنازلي، ويعرض **صافي ربح الكابتن بعد العمولة والوجهة قبل القبول**
- قبول آمن: إذا ضغط كابتنان بنفس اللحظة، واحد فقط ينجح (مُختبَر)
- ملاحة بـ Google Maps أو Waze، تنبيه تلقائي عند الوصول للزبون، و«وصلت» مرفوضة إذا كان بعيداً أكثر من 400 م
- عدّاد الانتظار المجاني ثم المدفوع، وتسجيل عدم حضور الزبون بعد انتهاء الوقت المجاني فقط
- «اسحب للتأكيد» لبدء وإنهاء الرحلة (يمنع الضغط بالغلط أثناء القيادة)
- الأجرة المتفق عليها ثابتة، ويُعاد حسابها فقط إذا زادت المسافة الفعلية عن 125% من التقدير
- عند الإنهاء: عمولة 7.5% تُخصم من محفظة الكابتن كحركة محاسبية مرة واحدة فقط (حتى مع ضغطتين)
- إذا اعتذر الكابتن، الطلب يرجع للبحث عن كابتن ثاني تلقائياً والعميل ما بيخسر طلبه
- الأرباح (اليوم / 7 أيام)، الهدف اليومي، المحفظة وحركاتها، طلب شحن رصيد عبر CliQ مع صورة الإشعار
- الكابتن تحت الحد الأدنى للرصيد (افتراضياً -3 دنانير) ما بيقدر يتصل ولا توصله طلبات

## ⏳ غير منجز بعد (المراحل القادمة)

| المرحلة | المحتوى |
|---|---|
| 8 | **موافقة الإدارة** على طلبات شحن CliQ (الطلب من الكابتن جاهز) |
| 9 | **لوحة الإدارة**: الخريطة الحية، التسعير، الكباتن، العملاء، التقارير، Audit Log |
| 10 | إشعارات Push |
| — | المحادثة داخل الرحلة، الحجز المسبق، المحطات المتعددة، الكوبونات، المناطق (Zones) |

الجداول الخاصة بهذه المراحل **موجودة في المخطط** (`wallets`, `wallet_transactions`,
`deposit_requests`, `messages`, `coupons`, `zones`, `audit_logs` …) حتى لا نحتاج
هجرات كبيرة لاحقاً، لكن لا يوجد كود يستخدمها بعد.

---

## ⚠️ حدود معروفة — بصراحة

1. **تطبيق الكابتن موقع ويب:** لازم يضل التطبيق مفتوح والشاشة شغالة حتى يوصل الموقع والطلبات.
   الآيفون ما بيسمح لمواقع الويب ترسل الموقع بالخلفية — هذا بينحل لما نحوّله لتطبيق حقيقي على App Store.
   **لوحة الإدارة لسا ما انبنت:** الموافقة على الكباتن تلقائية بفترة المعاينة (`CAPTAIN_AUTO_APPROVE`)،
   وطلبات شحن المحفظة بتضل «قيد المراجعة» لحد ما نبني اللوحة.
2. **رسائل OTP في وضع التطوير.** الرمز يظهر على الشاشة. يلزم ربط مزوّد SMS قبل أي استخدام حقيقي.
3. **المسافة المستخدمة في السعر تقديرية.** الخادم يحسبها هوائياً × معامل طرق 1.35.
   خط المسار على الخريطة يأتي من خادم OSRM **التجريبي** (للمعاينة فقط وليس للإنتاج)،
   وإذا تعذّر الوصول إليه يُرسم قوس منقّط تقريبي. للإنتاج يلزم خادم مسارات خاص أو مزوّد مدفوع.
4. **ترخيص الخرائط.** بلاطات CARTO مجانية للتجربة؛ قبل الإطلاق التجاري يلزم ترخيص منهم
   أو تبديل المزوّد (Google Maps / Mapbox / MapTiler). التبديل متغيّر بيئة واحد ولا يحتاج تعديل كود.
5. **التحديث بالاستعلام الدوري** (كل 3 ثوانٍ) وليس WebSocket. كافٍ الآن، ويجب استبداله
   بـ WebSocket قبل تشغيل عدد كبير من الكباتن.
6. **حد الطلبات في الذاكرة** — يعمل على خادم واحد فقط. عند التوسع لعدة خوادم يلزم Redis.
7. **الدفع نقداً فقط.** بنية الدفع مجهّزة لإضافة بوابة لاحقاً، ولا يوجد ادعاء بوجود دفع إلكتروني.

---

## 🔑 المتغيرات المطلوبة للإنتاج

| المتغير | مطلوب؟ | الوصف |
|---|---|---|
| `NODE_ENV` | نعم | `production` |
| `JWT_SECRET` | نعم | قيمة عشوائية ≥ 24 حرفاً (الخادم يرفض الإقلاع بدونها) |
| `DATABASE_URL` | نعم | رابط PostgreSQL من Neon |
| `SMS_PROVIDER` | نعم | `twilio` أو مزوّد محلي |
| `CAPTAIN_AUTO_APPROVE` | للمعاينة فقط | `1` = الكابتن يتفعّل بدون مراجعة |
| `TWILIO_*` | عند استخدام Twilio | بيانات الحساب |
| `MAP_TILES_URL` / `MAP_TILES_URL_DARK` | لا | لتبديل مزوّد الخرائط (نهاري / ليلي) |
| `ROUTING_URL` | للإنتاج | خادم مسارات OSRM خاص |
| `UPLOADS_DIR` | لا | مسار صور الحسابات (يفضّل قرص دائم) |

---

## 🚀 خطوات النشر باختصار

1. أنشئ قاعدة بيانات على Neon وانسخ `DATABASE_URL`.
2. ارفع المشروع إلى GitHub.
3. Render → New Web Service → Build: `npm install` · Start: `npm start`.
4. أضف المتغيرات أعلاه.
5. افتح الرابط — الجداول والبيانات الأساسية تُنشأ تلقائياً.

التفاصيل الكاملة في [`README.md`](./README.md).
